# Changes that scripts/mutate can make without altering behaviour.
# One per line: file:line from->to, two spaces, and why it alters nothing.
# A line here that the tests now notice is reported as out of date, so
# this file cannot quietly excuse a change made later at the same place.

internal/policy/policy.go:186 > becomes >=  picking the last of several equal ranks gives the same level as picking the first
internal/security/security.go:180 > becomes >=  the length cap is applied either way at exactly the cap
